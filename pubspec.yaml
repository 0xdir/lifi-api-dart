name: lifi_api_dart
description: A Future-based library to interact with the Li.Fi API, allowing easy swapping of tokens between blockchain networks with Dart.
version: 0.1.2
repository: https://github.com/0xdir/lifi-api-dart

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.0.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0